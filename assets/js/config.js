/* Public config (safe) — edit URLs only */
window.ECHO_CONFIG = {
  // Where the "Login / Start" goes.
  login_url: "/cabinet/",
  // Stable API domain on Vercel:
  api_base: "https://api.echoproject.space",
  sentry_dsn: "",
  plausible_domain: ""
};
